package com.nx.main;

/** Compile-time only stub. The real class remains in the untouched original classes.dex. */
public class App extends android.app.Application {
    @Override public void onCreate() { super.onCreate(); }
}
