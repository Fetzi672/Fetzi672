<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="fac.guard">
    <uses-sdk android:minSdkVersion="23" android:targetSdkVersion="28" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <queries>
        <package android:name="com.cocfz.com.freescript" />
    </queries>

    <application
        android:allowBackup="false"
        android:label="FAC Guard V15.2.1"
        android:usesCleartextTraffic="false"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <activity
            android:name="fac.guard.MainActivityV152"
            android:exported="true"
            android:launchMode="singleTask" />

        <activity-alias
            android:name="fac.guard.MainActivity"
            android:targetActivity="fac.guard.MainActivityV152"
            android:enabled="true"
            android:exported="true" />

        <!-- Setup-only launcher entry. It is disabled after the first successful
             FAC authorization/runtime setup while the Guard itself stays active. -->
        <activity-alias
            android:name="fac.guard.LauncherAlias"
            android:targetActivity="fac.guard.MainActivityV152"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <service
            android:name="fac.guard.GuardService"
            android:exported="false"
            android:stopWithTask="false" />

        <service
            android:name="fac.guard.TextMaskAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/fac_text_mask_service" />
        </service>

        <receiver
            android:name="fac.guard.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
